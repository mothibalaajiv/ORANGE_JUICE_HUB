# Tangy Town - Fresh Juice Delivery Platform
# Production Requirements for Python 3.11.9
# Clean, minimal dependencies - only what's actually used

# =============================================================================
# CORE FLASK FRAMEWORK
# =============================================================================
Flask==3.0.0
Flask-Cors==4.0.0
Flask-SocketIO==5.3.5
python-socketio==5.10.0

# =============================================================================
# DATABASE & DATA STORAGE
# =============================================================================
pymongo==4.6.3
certifi==2024.2.2

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
PyJWT==2.8.0
bcrypt==4.1.2

# =============================================================================
# CONFIGURATION & ENVIRONMENT
# =============================================================================
python-dotenv==1.0.0

# =============================================================================
# PAYMENT PROCESSING
# =============================================================================
razorpay==1.4.1

# =============================================================================
# PRODUCTION SERVER
# =============================================================================
gunicorn==21.2.0

# =============================================================================
# FLASK DEPENDENCIES (Auto-installed with Flask)
# =============================================================================
Werkzeug==3.0.1
click==8.1.7
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3
blinker==1.7.0

# =============================================================================
# SOCKET.IO DEPENDENCIES
# =============================================================================
eventlet==0.33.3
greenlet==3.0.2

# =============================================================================
# MONGODB DEPENDENCIES
# =============================================================================
dnspython==2.4.2

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 1. This file contains only essential dependencies actually used in the codebase
# 2. Analytics features (pandas, reportlab) are disabled and not included
# 3. Background jobs use threading.Timer instead of APScheduler
# 4. Geospatial operations use custom math calculations instead of geopy
# 5. No caching (redis) or validation (marshmallow) dependencies
# 6. All versions pinned for stability and reproducibility
# 7. Python 3.11.9 is required for deployment
# 8. Standard library modules (datetime, os, math, threading, ssl, hmac, hashlib) not listed